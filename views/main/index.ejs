<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Undertale Quiz – Welcome</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/quizStyle.css">
</head>
<body>
<%- include('./header') %>
<div id="welcome-box" class="quiz container">
    <h1>★ Welcome to the Quiz ★</h1>
    <p>Test your knowledge… or perish trying.</p>

    <form id="trivia-form"
          action="/quizgame"
          method="GET"
          class="row gy-3 justify-content-center mt-4">

        <div class="col-12 col-md-6">
            <label for="amount" class="form-label fw-semibold">Questions</label>
            <input  type="number"
                    id="amount"
                    name="amount"
                    class="form-control"
                    value="10"
                    min="1"
                    max="50"
                    required>
        </div>

        <div class="col-12 col-md-6">
            <label for="category" class="form-label fw-semibold">Category</label>
            <select id="category" name="category" class="form-select">
                <option value="">Any Category</option>
            </select>
        </div>

        <div class="col-12 col-md-6">
            <label for="difficulty" class="form-label fw-semibold">Difficulty</label>
            <select id="difficulty" name="difficulty" class="form-select">
                <option value="">Any</option>
                <option value="easy">Easy</option>
                <option value="medium">Medium</option>
                <option value="hard">Hard</option>
            </select>
        </div>
        <div class="col-12">
            <button id="start-btn"
                    type="submit"
                    class="btn btn-primary px-4">
                Start Quiz
            </button>
        </div>
    </form>

    <small class="text-muted d-block mt-2">
        Questions provided by
        <a href="https://opentdb.com/api_config.php" target="_blank" rel="noopener">
            Open Trivia DB
        </a>
    </small>
</div>
<script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
></script>
<script src="/scripts/trivia.js"></script>
</body>
</html>